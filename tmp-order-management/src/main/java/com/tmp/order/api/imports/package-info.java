/**
 * Public Order Intake import contracts (source-neutral model, preview, confirm).
 *
 * <p>Callers (STXT adapter, Import UI) depend only on this package. No JavaFX, parser, Firebird,
 * or persistence types are exposed here (ADR-029).
 */
package com.tmp.order.api.imports;
