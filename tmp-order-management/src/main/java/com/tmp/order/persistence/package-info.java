/**
 * Internal persistence layer of the Order Management Capability.
 *
 * <p>Reserved for hand-written JDBC ports and adapters over the {@code order_management} schema
 * (no JPA). No type in this package is visible outside {@code tmp-order-management}.
 */
package com.tmp.order.persistence;
