spring:
  config:
    activate:
      on-profile: test
  flyway:
    enabled: true
    locations: classpath:db/migration

tmp:
  capability:
    sample:
      diagnostic: true

logging:
  level:
    root: WARN
    com.tmp: INFO
