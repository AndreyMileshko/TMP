spring:
  config:
    activate:
      on-profile: test
  flyway:
    enabled: true
    locations: classpath:db/migration

tmp:
  capability:
    sample:
      diagnostic: true
  security:
    bootstrap:
      admin-login: admin
      admin-display-name: Administrator
      admin-password: test-admin-password

logging:
  level:
    root: WARN
    com.tmp: INFO
