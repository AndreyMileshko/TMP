spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tmp_test
    username: tmp_test
    password: tmp_test
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: tmp-test-pool
      maximum-pool-size: 5
  flyway:
    enabled: true
    locations: classpath:db/migration
