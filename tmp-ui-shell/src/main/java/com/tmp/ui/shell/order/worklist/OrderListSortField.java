package com.tmp.ui.shell.order.worklist;

/**
 * Explicit Orders worklist sort fields. Sort is applied to the full matched set before pagination.
 */
public enum OrderListSortField {
    ORDER_NUMBER,
    CUSTOMER,
    CREATED_AT,
    ITEM_COUNT,
    STATUS
}
